var a = Number(prompt('indtast et tal'));
function addVat() {
return (a * 1.25);
}

console.log(addVat(a));

function subVat() {
    return (a * 0.8);
    }
    console.log(subVat(a));

    function calVat() {
        return (a * 0.25);
        }

    console.log(calVat(a));
